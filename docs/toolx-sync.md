# `toolx-sync` documentation moved

> Audience: users following an older ToolX link
> Status: Relocation notice; not a source of truth
> Applies to: compatibility period beginning with `v0.3.2`
> Source of truth for: none

The canonical reference is now [`docs/cli/toolx-sync.md`](cli/toolx-sync.md).
This compatibility page intentionally does not duplicate the reference.
